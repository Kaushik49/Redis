# 🚀 Basic Redis Implementation

This project demonstrates a **basic implementation of Redis** for caching and data storage.  
Redis is an in-memory key-value store often used for caching, session management, and real-time applications.

---

## 📂 Project 
├── express server       # backend server using redis library
├── worker   # dummy worker to connect to the express server
└── README.md       # Documentation
